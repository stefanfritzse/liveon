apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longevity-app
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: longevity-app
                port:
                  number: 80
