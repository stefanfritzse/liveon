apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longevity-app
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: longevity-app
      port:
        number: 80
