# Runtime dependencies for the Live On web app and content pipelines.
# Development-only tooling lives in requirements-dev.txt at the repo root.
fastapi==0.115.2
feedparser==6.0.11
httpx==0.28.1
jinja2==3.1.4
langchain-core==0.3.77
# Pinned: an unpinned minor release used to be able to land silently.
langchain-community==0.3.30
# The maintained Ollama client; without it the community build is used and
# emits a deprecation warning on every model construction.
langchain-ollama==0.3.10
markdown==3.10
PyYAML==6.0.2
uvicorn[standard]==0.30.1
